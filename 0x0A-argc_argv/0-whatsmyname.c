#include <stdio.h>
#include <stdlib.h>

/**
  * main - Prints its name.
  * @argc: Argument count.
  * @argv: Array of arguments.
  *
  * Return: Always 0
  */

int main (int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
